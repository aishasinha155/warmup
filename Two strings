string twoStrings(string s1, string s2) {
    string sum="";
    
    for(int j=0;j)
    
    int l=s2.length();
     for(int i=0;i<l;i++){
     sum=sum+s2[i];
      char b[100]=sum;
     if(strstr(a,b)!=NULL)
     return "YES";
     else
      return "NO";
 }
}
